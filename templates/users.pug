extends base.pug
block title
  | Users
block body
  | {% if req.status_code == 200 %}
  .row
    form.col.l3(action="" method='post')
      .row
        .input-field.col.s12
          input#role_name.validate(placeholder='Enter A New User Name', type='text', name="user_name")
      .row
        button.btn.waves-effect.waves-light(type='submit', name='action')
          | Create User
          i.material-icons.right send

    .col.l9
      ul.collapsible(data-collapsible="expandable")
        | {% for item in req.json()['data']['rows'] %}
        li
          .collapsible-header {{item.firstname}} {{item.lastname}}
            span.right
              a.waves-effect.waves-light.btn(href="{{url_for('users_specific', id=item.id)}}") DELETE
          .collapsible-body {% for k,v in item.items() %}
            p {{ "{}: {}".format(k,v) }}
            {% endfor %}

        | {% endfor %}
        | {% else %}
        | {% endif %}
    